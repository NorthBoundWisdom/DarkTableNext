interface:
  display_name: "Review and Commit"
  short_description: "Review, validate, and commit repository changes"
  default_prompt: "Use $review-and-commit to review all current changes, run proportional validation, and create an intentional commit."
